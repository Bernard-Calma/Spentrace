import { StyleSheet, Text } from "react-native"

const Header = () => {
    return(
        <Text style={{
            fontFamily: "roboto-bold", 
            fontWeight: "bold", 
            fontSize: 32,
            marginTop: "10%"
        }}>Spentrace</Text>
    )
}

export default Header;